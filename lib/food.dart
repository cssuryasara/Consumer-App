class FoodPass {
  final String fname, img, canteeid, canteename,desc;
  int fprice;
  final double fRating;
  final bool veg;
  FoodPass(this.fname, this.fprice, this.fRating, this.img, this.veg,
      this.canteeid, this.canteename,this.desc);
}
